# Pinned for Python 3.12. Bump deliberately, not via `pip install -U`.

# --- web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- database ---
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.3.6

# --- background jobs ---
celery==5.4.0
redis==5.2.1

# --- auth (Clerk JWT verification + webhook signatures) ---
pyjwt[crypto]==2.10.1
svix==1.61.0

# --- billing ---
stripe==11.4.1

# --- http client ---
httpx==0.28.1

# --- document text extraction (Phase 4 Task 1: RAG document ingestion) ---
# pypdf, not PyMuPDF: PyMuPDF is faster/more capable but AGPL-licensed
# unless a commercial license is purchased — a real risk for commercial
# SaaS, not something to pull in without that being a deliberate call.
# pypdf is pure Python, no system dependencies, BSD-licensed.
pypdf==6.16.2
python-docx==1.2.0

# --- PDF generation (Phase 7 Task 3: preaching-format export) ---
# WeasyPrint, not Playwright/headless-Chromium: the manuscript is static
# server-known text with no JS/charts to render, so a ~300MB browser
# would be pure overhead — WeasyPrint is HTML+CSS -> PDF directly,
# actively maintained (releases every 2-3 months), BSD-3-Clause (same
# license bar that ruled out PyMuPDF above). Needs real system packages
# (Pango/Cairo/GDK-PixBuf — see Dockerfile) since those can't be
# installed via pip; that's a normal Docker-build-time apt-get, not
# blocked by this project's dev-sandbox no-sudo constraints elsewhere.
weasyprint==69.0

# --- resilience ---
tenacity==9.0.0

# --- testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
